fastapi>=0.138.0
uvicorn>=0.49.0
stockfish>=5.2.0
pydantic>=2.13.4
faster-whisper>=1.2.1
# Powers the local edge-tts bridge (/tts): serves Azure-quality Burmese neural
# voices (my-MM-NilarNeural) without needing an Azure account or API key.
edge-tts>=7.0.0
# Optional — powers the Logician (Prolog) panel; the app degrades gracefully
# without them.
pyswip>=0.3.0
python-chess>=1.999
